#include <stdio.h>

int main() {
    int a,b;
    scanf("%d %d",&a,&b);
    if ( (a%b==0 && a%b==1) ||(b%a==0 && b%a==1 )){
        printf("Yes");
    }
    else if {
        printf("No");
    }
    return 0;
}